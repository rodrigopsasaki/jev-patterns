---
"jev-patterns": minor
---

Add `measureThresholds()` for measuring coverage, correctness, precision, Wilson score intervals, and AUROC across caller-supplied score floors. The caller remains responsible for choosing a floor and re-validating it for each judge model.
